# SalesAgent — chat-driven sales rep agent
# Orchestrator loop
# Pinned to the exact set that passed the sonnet-5 bind_tools smoke (2026-08-05)
langgraph==1.2.4
langgraph-checkpoint-sqlite==3.1.1   # 2.x calls serializer APIs langgraph-checkpoint 4.x removed
aiosqlite==0.20.0
langchain-anthropic==1.5.4
langchain-core==1.5.3
# Raw SDK for in-tool Haiku extraction (claude_cached)
anthropic==0.120.2
# Web
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2
python-multipart
# Data / IO
requests>=2.31
python-dotenv>=1.0
openpyxl>=3.1
pdfplumber>=0.11
# Anti-bot HTML fetching (branch scrape, fetch_page); optional import at runtime
curl_cffi>=0.7
